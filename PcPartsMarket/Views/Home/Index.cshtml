@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered Components</h2>
<h2></h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var pcComponent in Model.PreferredPcComponents)
    {
        @await Html.PartialAsync("PcComponentsSummary", pcComponent)
    }
</div>



